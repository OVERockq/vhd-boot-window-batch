# CreateVhdBoot_UEFI_BIOS.cmd

### 📦 개요
이 스크립트는 **WinPE 환경에서 실행되는 VHD 부팅 디스크 자동 구성 도구**입니다.  
UEFI / BIOS(레거시) 모드를 자동 감지하여, 하나의 스크립트로 모든 환경에서 부팅 가능한 디스크를 구성합니다.

---

### ⚙️ 주요 기능
- Disk 0 완전 초기화 및 GPT 변환
- EFI(100MB FAT32) + Windows(NTFS) 파티션 자동 생성
- 지정된 VHD 파일 복사 및 자동 부팅 구성
- BIOS / UEFI 자동 감지 및 `bcdboot` 구성 자동 분기
- 실행 전 **데이터 삭제 경고 및 확인 절차(YES 입력)**

---

### 🧭 사용법

1. WinPE 환경에서 부팅합니다.
2. 스크립트를 USB 등에서 실행합니다.
   ```bat
   X:\> CreateVhdBoot_UEFI_BIOS.cmd
